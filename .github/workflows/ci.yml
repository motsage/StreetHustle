name: Unity CI/CD - Personal (Windows Only)

on:
  push:
    branches: [ "develop", "main" ]

jobs:
  build-and-email:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # Build Windows Standalone without license activation
      - name: Build Windows
        uses: game-ci/unity-builder@v2
        with:
          unityVersion: 6000.0.55f1
          targetPlatform: StandaloneWindows64
          buildName: GameBuild
          buildPath: build/Windows
          activateLicense: false   # skip license

      # Zip the build
      - name: Zip Build
        run: |
          mkdir output
          zip -r output/Game-Build-Windows.zip build/Windows

      # Upload build artifact
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Game-Build
          path: output/Game-Build-Windows.zip

      # Send email notification
      - name: Send Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "CI Build - ${{ github.repository }}"
          to: "team@example.com"
          from: "CI Bot <ci@example.com>"
          body: |
            A new push triggered a Unity build.

            Repo: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.sha }}

            Windows build is ready for testing.
          attachments: output/Game-Build-Windows.zip
